---
- name: Prepare
  hosts: all
  become: true
  tasks:
    - name: Create man1 directory required by Java post-install
      ansible.builtin.file:
        path: /usr/share/man/man1
        state: directory
        mode: "0755"
